## 1. 实现启动加载界面

- [x] 1.1 在 `src/renderer/index.html` 的 `<head>` 中添加内联 CSS 样式
- [x] 1.2 在 `src/renderer/index.html` 的 `<body>` 中添加加载容器 HTML 结构
- [x] 1.3 在 `src/renderer/main.tsx` 中添加加载容器移除逻辑
- [x] 1.4 验证内联样式与 shadcn/ui 主题色系一致性

## 2. 测试验证

- [ ] 2.1 在 Windows 上验证启动加载效果
- [ ] 2.2 在 macOS 上验证启动加载效果
- [ ] 2.3 在 Linux 上验证启动加载效果
- [ ] 2.4 验证加载容器在 React 挂载后正确移除
- [ ] 2.5 验证加载动画流畅度

## 3. 文档更新

- [ ] 3.1 更新 `openspec/specs/electron-app/spec.md`（归档后）
